@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Create Staff"];
}

<h2>@Localizer["Create Staff"]</h2>

<form method="post">
    @Html.AntiForgeryToken()

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    @if (ViewBag.PasswordPolicy != null)
    {
        <div class="alert alert-info">@ViewBag.PasswordPolicy</div>
    }

    <label>@Localizer["Name"]</label>
    <input name="StaffName" class="form-control" value="@Model?.StaffName" />

    <label>@Localizer["Phone"]</label>
    <input name="Phone" class="form-control" value="@Model?.Phone" />

    <label>@Localizer["Email"]</label>
    <input name="Email" class="form-control" value="@Model?.Email" />

    <hr />

    <h4>@Localizer["Account"]</h4>

    <label>@Localizer["Username"]</label>
    <input name="username" class="form-control" />

    <label>@Localizer["Password"]</label>
    <input name="password" type="password" class="form-control" />

    <label>@Localizer["Confirm Password"]</label>
    <input name="confirmPassword" type="password" class="form-control" />

    <br />

    <button class="btn btn-success">@Localizer["Save"]</button>
</form>
